import com.goomo.Components.Processor;

public class MainClass {
	public static void main(String[] args) {
		/*
		 * Laptop laptop=new Laptop();
		 * System.out.println(laptop.getProcessor().getBrand());
		 */
		Processor graphiccard=new Processor("Intel", series, generation, cores, threads, cashMemory, frequency, minFrequency, maxFrequency)
		Laptop laptop=new Laptop(screen, processor, ram, hardDrive, graphiccard, opticalDrive, keyBoard);
}
}
